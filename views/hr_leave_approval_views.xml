<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Tree View for Waiting My Approval -->
    <record id="hr_leave_view_tree_waiting_my_approval" model="ir.ui.view">
        <field name="name">hr.leave.view.tree.waiting.my.approval</field>
        <field name="model">hr.leave</field>
        <field name="arch" type="xml">
            <tree string="Waiting My Approval" sample="1">
                <field name="employee_id" widget="many2one_avatar_user"/>
                <field name="department_id" optional="show"/>
                <field name="holiday_status_id" class="fw-bold"/>
                <field name="name"/>
                <field name="date_from"/>
                <field name="date_to"/>
                <field name="duration_display" string="Duration"/>
                <field name="state" widget="badge" decoration-info="state == 'draft'" decoration-warning="state in ('confirm','validate1')" decoration-success="state == 'validate'"/>
                <field name="can_approve" column_invisible="True"/>
                <field name="can_reset" column_invisible="True"/>
                <button string="Approve" name="action_approve" type="object"
                    icon="fa-thumbs-up"
                    invisible="state != 'confirm' or not can_approve"
                    class="btn-success"/>
                <button string="Validate" name="action_validate" type="object"
                    icon="fa-check"
                    invisible="state != 'validate1' or not can_approve"
                    groups="hr_holidays.group_hr_holidays_user"/>
                <button string="Refuse" name="action_refuse" type="object"
                    icon="fa-times"
                    invisible="state not in ('confirm', 'validate1') or not can_approve"
                    class="btn-danger"/>
                <button string="Mark as Draft" name="action_draft" type="object"
                    icon="fa-rotate-left"
                    invisible="state not in ('confirm', 'refuse') or not can_reset"/>
            </tree>
        </field>
    </record>

    <!-- Action for Waiting My Approval -->
    <record id="action_hr_leave_waiting_my_approval" model="ir.actions.act_window">
        <field name="name">Waiting My Approval</field>
        <field name="res_model">hr.leave</field>
        <field name="view_mode">tree,kanban,form,calendar,activity</field>
        <field name="domain">['&amp;', ('state', 'in', ['confirm', 'validate1']), ('employee_id.parent_id.user_id', '=', uid)]</field>
        <field name="groups_id" eval="[(6, 0, [ref('infs_time_off.group_leave_parent_approver')])]"/>
        <field name="context">{
            'search_default_approve': 1,
            'search_default_second_approval': 1,
            'hide_employee_name': 0,
            'holiday_status_display_name': False
        }</field>
        <field name="search_view_id" ref="hr_holidays.view_hr_holidays_filter"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No leave requests waiting for your approval
            </p>
            <p>
                Leave requests from your team will appear here when they need your approval.
            </p>
        </field>
    </record>

    <!-- Action View References -->
    <record id="action_hr_leave_waiting_my_approval_view_tree" model="ir.actions.act_window.view">
        <field name="sequence" eval="1"/>
        <field name="view_mode">tree</field>
        <field name="view_id" ref="hr_leave_view_tree_waiting_my_approval"/>
        <field name="act_window_id" ref="action_hr_leave_waiting_my_approval"/>
    </record>

    <record id="action_hr_leave_waiting_my_approval_view_kanban" model="ir.actions.act_window.view">
        <field name="sequence" eval="2"/>
        <field name="view_mode">kanban</field>
        <field name="view_id" ref="hr_holidays.hr_leave_view_kanban_approve_department"/>
        <field name="act_window_id" ref="action_hr_leave_waiting_my_approval"/>
    </record>

    <record id="action_hr_leave_waiting_my_approval_view_form" model="ir.actions.act_window.view">
        <field name="sequence" eval="3"/>
        <field name="view_mode">form</field>
        <field name="view_id" ref="hr_holidays.hr_leave_view_form"/>
        <field name="act_window_id" ref="action_hr_leave_waiting_my_approval"/>
    </record>

    <!-- Top-Level Menu Item: Waiting My Approval -->
    <record id="menu_hr_leave_waiting_my_approval" model="ir.ui.menu">
        <field name="name">Waiting My Approval</field>
        <field name="parent_id" ref="hr_holidays.menu_hr_holidays_root"/>
        <field name="action" ref="action_hr_leave_waiting_my_approval"/>
        <field name="sequence">2</field>
        <field name="groups_id" eval="[(6, 0, [ref('infs_time_off.group_leave_parent_approver')])]"/>
    </record>

</odoo>
