<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">

    <!-- Modified TimeOffCard: uses duration_display from Python when available -->
    <div t-name="infs_time_off.TimeOffCard" class="o_timeoff_card py-3 text-primary">
        <t t-set="data" t-value="props.data"/>
        <t t-set="duration" t-value="props.requires_allocation ? data.virtual_remaining_leaves : data.virtual_leaves_taken"/>
        <t t-set="parsed_duration" t-value="formatNumber(this.lang, duration)"/>
        <t t-set="show_popover" t-value="true"/>

        <strong class="o_timeoff_name"><t t-esc="props.name"/></strong>
        <span class="o_timeoff_duration">
            <t t-if="data and data.icon">
                <img t-att-src="data.icon" />
            </t>
            <span t-att-style="data.holds_changes ? 'color: #B6D369;' : ''">
                <t t-if="data.duration_display" t-esc="data.duration_display"/>
                <t t-else="" t-esc="parsed_duration"/>
            </span>
        </span>
        <span
            t-att-class="'text-uppercase o_timeoff_details p-1' + (warning ? ' alert alert-warning' : '')"
            t-on-click.stop="(ev) => (show_popover &amp;&amp; this.onClickInfo(ev))">
            <t t-if="!data.duration_display">
                <t t-if="data.request_unit == 'hour'">hours</t>
                <t t-else="">days</t>
            </t>
            <t t-if="props.requires_allocation">
                available
            </t>
            <t t-else="">
                taken
            </t>
            <span t-if="show_popover"
                t-att-class="'o_timeoff_info fa' + (warning ? ' fa-exclamation-triangle' : ' fa-question-circle-o')"
            />
        </span>
        <span t-if="props.requires_allocation and data.allocation_details" class="text-uppercase o_timeoff_validity">
            <t t-foreach="data.allocation_details" t-as="alloc" t-key="alloc_index">
                (<t t-esc="alloc.remaining_display"/> valid until <span t-esc="alloc.expire"/>)
            </t>
        </span>
        <span t-elif="props.requires_allocation and data.closest_allocation_expire !== false" class="text-uppercase o_timeoff_validity">
            (valid until <span t-esc="data.closest_allocation_expire"/>)
        </span>
    </div>

    <!-- Modified TimeOffCardMobile -->
    <t t-name="infs_time_off.TimeOffCardMobile">
        <t t-set="data" t-value="props.data"/>
        <t t-set="duration" t-value="props.requires_allocation ? data.virtual_remaining_leaves : data.virtual_leaves_taken" />

        <span class="float-end o_timeoff_card_mobile">
            <t t-if="props.requires_allocation">
                <t t-if="data.duration_display">
                    <strong t-esc="data.duration_display" class="o_timeoff_green text-success"/>
                </t>
                <t t-else="">
                    <strong t-esc="duration" class="o_timeoff_green text-success"/> / <span t-esc="data.max_leaves"/> <t t-if="data.request_unit == 'hour'">Hours</t><t t-else="">Days</t>
                </t>
                <span class="o_timeoff_green text-success">Available</span>
            </t>
            <t t-else="">
                <strong t-esc="duration"/> <t t-if="data.request_unit == 'hour'">Hours</t><t t-else="">Days</t> <span class="text-primary">Taken</span>
            </t>
        </span>
    </t>

</templates>
